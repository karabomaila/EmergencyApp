<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emerg">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Emergency-app"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HospitalLocation"
            android:parentActivityName = ".HospitalHelpline"/>
        <activity android:name=".HospitalForm"
            android:label="Hospital Form"
            android:parentActivityName = ".MainPage"/>

        <activity android:name=".HospitalHelpline"
            android:parentActivityName = ".MainPage"/>

        <activity android:name=".FireBrigade"
            android:parentActivityName = ".MainPage"/>

        <activity android:name=".PoliceHelpline"
            android:parentActivityName = ".MainPage"/>
        <activity android:name=".MainPage" />

        <activity android:name=".CreateAccount" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>